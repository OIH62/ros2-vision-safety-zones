# Models

Model weights are intentionally not versioned. By default Ultralytics resolves
`yolov8n-pose.pt` on first use. For offline deployment, put an appropriately
licensed model at a deployment-controlled path and pass `model_path:=...`.
